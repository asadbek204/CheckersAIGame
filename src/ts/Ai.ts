import Player from "./Player.js";
import { Color } from "./Game.js";

export default class AIPlayer extends Player {
    setQueue(): string {
        return 'I\'m AI I\'m not working'
    }
}

export {}